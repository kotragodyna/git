# Simple CLI Calculator App

This is a basic CLI calculator app that supports addition and subtraction operations.

## Usage

To use the calculator, run the `app.py` script with the following format:

```bash
python app.py <operation> <operand1> <operand2>
